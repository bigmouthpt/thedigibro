services:
  - type: web
    name: newsletter-tone-bot-beta
    runtime: docker
    plan: starter
    autoDeploy: true
    envVars:
      - key: OPENAI_API_KEY
        sync: false
      - key: CORPUS_PATH
        value: data/corpus.json
